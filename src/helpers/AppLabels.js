//import {useTranslation } from "react-i8next";

export default function AppLabels() {
    //const {t, i8n } = useTranslation();
    return Object.freeze({
        REGISTRATION_PROMPT: "HI! We are so excited you chose PAA to help you with all you medical needs. Before continuing please regster as either a Doctor or a Patient. Once you have chosen you will need to fill out one of the below forms ",
    });
}